import React, { Component } from 'react'

import {
    View,
    Text,
    TouchableOpacity,
    Image,
    TouchableHighlight,
    ImageBackground
} from 'react-native'

const Arrow = props => {
    const {

    } = props;

    return (

        <Component
            style={[styles.container]}
            onPress={onPress}
            {...attributes}
        >
        </Component>

    )
};
export default Arrow;